[package]
name = "mas-project-rust"
version = "0.1.0"
edition = "2024"
description = "Rust implementation of the MAS emergent-language simulation"
license = "MIT"

[dependencies]
ndarray = { version = "0.17", features = ["rayon"] }
serde_json = "1.0"
